<% include header-admin %>
	<div class="section-1">
			<ul class="list">
				<li><a href="/admin">Главная</a></li>
				<li class="active"><a href="/admin/news"><i class="th-pin"></i> Записи</a></li>
				<li><a href="/admin/actors"><i class="th-group"></i> Актеры</a></li>
				<li><a href="/admin/imgs"><i class="th-picture"></i> Медиафайлы</a></li>
			</ul>
		</div>
		<div class="section-2">
			<h2>Записи</h2>
			<div class="element-list">
				<h3>Новости</h3>
				<a href="/admin/news/add">
					<div class="list-element">
						<div class="plus"><span></span><span></span></div>
						<div class="text">
							<a href="/admin/news/add" class="href-btn ok">Добавить</a>
						</div>
					</div>
				</a>
				<div class="list-element">
					<div class="text">
						<span>Ближайшее выступление</span>
					</div>
					<select class="select-soon-scene" name="soon-scene" onchange="changeSoonScene(this)">
						<% news.forEach( (post) => { %>
							<option
							<% if (post.fav) { %>
								selected="selected"
							<% } %>
							value="<%= post.id%>"><%= post.title%></option>
						<% }); %>
					</select>
				</div>
				<% news.forEach( (post) => { %>
							<div class="list-element">
								<img src="<%= post.img %>" alt="img"/>
								<div class="text">
									<h4><%= post.title %></h4>
									<p><%= post.text.substring(0,100) %>...</p>
									<p class="date"><%= moment(post.date).format('DD-MM-YYYY') %></p>
									<a href="/admin/news/edit/<%= post.id %>" class="href-btn ok"><i class="th-pencil"></i> Изменить</a>
									<button class="href-btn danger" onclick="deleteNews(<%= post.id %>)"><i class="th-trash"></i> Удалить</button>
								</div>
							</div>
				<% }); %>

			</div>
		</div>
<% include footer-admin %>
